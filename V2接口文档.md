# 票务服务端 V2 API 文档

本文档仅描述 `/v2` 接口，以当前服务端实现为准。旧版接口请参阅 `接口文档.md`。

## 1. 基本信息

| 项目 | 约定 |
|---|---|
| API 前缀 | `/v2` |
| 客户端接口格式 | AES-CBC 加密后的 JSON `payload` 外壳 |
| 管理员接口格式 | 普通 JSON + Bearer Token |
| 字符编码 | UTF-8 |
| 时间字段 | Unix 秒级时间戳 |
| Content-Type | `application/json` |

示例地址：

```text
http://127.0.0.1:8000/v2
```

部署环境请将主机和端口替换为实际地址。

## 2. 客户端加密协议

### 2.1 请求外壳

所有 V2 客户端接口只接收以下外层 JSON：

```json
{
  "payload": "AES-CBC 加密后的 Base64 字符串"
}
```

`api_key`、`device_id`、`match_id`、`task_id`、`order_ip` 等业务参数必须放在 `payload` 解密后的 JSON 对象中，不通过 Header、Path 或 Query 传递。

外层和解密后的业务对象均不允许出现未定义字段。

### 2.2 加密参数

| 项目 | 约定 |
|---|---|
| 算法 | AES-CBC |
| Padding | PKCS#7 |
| Key | 环境变量 `AES_KEY` 的 UTF-8 字节 |
| IV | 环境变量 `AES_IV` 的 UTF-8 字节 |
| 明文 | UTF-8 JSON 对象 |
| 密文 | Base64 字符串 |

长度要求：

- `AES_KEY`：16、24 或 32 字节。
- `AES_IV`：16 字节。

客户端处理流程：

1. 将业务参数序列化为 UTF-8 JSON。
2. 使用 PKCS#7 补齐。
3. 使用 AES-CBC 和约定的 Key、IV 加密。
4. 将密文 Base64 编码后放入 `payload`。
5. 收到响应后，对响应 `payload` 执行相反过程。

HTTPS 可以与应用层 AES-CBC 同时使用，但不能替代本文档定义的 `payload` 外壳。

### 2.3 加密响应

客户端接口成功响应和业务错误响应均为：

```json
{
  "payload": "AES-CBC 加密后的 Base64 字符串"
}
```

解密后统一为：

```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

客户端应同时判断 HTTP 状态码和解密后的 `code`。

### 2.4 无效密文响应

请求缺少 `payload`、外壳格式错误或无法解密时，服务端返回 HTTP `400`，该响应不加密：

```json
{
  "code": -4001,
  "data": null,
  "msg": "invalid encrypted request"
}
```

### 2.5 解密后参数校验错误

密文可以正常解密，但业务参数缺失、类型错误或包含未知字段时，服务端返回 HTTP `422`。响应仍然加密，解密后示例：

```json
{
  "code": -422,
  "msg": "invalid request",
  "data": {
    "errors": [
      {
        "field": "task_id",
        "message": "Field required",
        "type": "missing"
      }
    ]
  }
}
```

## 3. 客户端接口总览

| 方法 | 路径 | 成功状态 | 说明 |
|---|---|---:|---|
| `POST` | `/v2/auth` | `200` | 授权并绑定或更新设备 |
| `POST` | `/v2/matches/current/config` | `200` | 获取当前比赛配置和公告 |
| `POST` | `/v2/matches/detail` | `200` | 获取指定比赛详情 |
| `POST` | `/v2/tasks/check` | `200` | 校验用户和设备绑定关系 |
| `POST` | `/v2/orders` | `201` | 上报订单 |

`POST /v2/matches/config` 是当前比赛配置接口的兼容别名，新客户端应使用 `/v2/matches/current/config`。

## 4. 客户端授权

`POST /v2/auth`

### 解密后请求参数

| 字段 | 类型 | 必填 | 约束 | 说明 |
|---|---|---:|---|---|
| `api_key` | string | 是 | 1-64 字符 | 用户密钥 |
| `device_id` | string | 是 | 1-128 字符 | 客户端设备标识 |
| `device_name` | string/null | 否 | 最长 128 字符 | 设备名称 |

```json
{
  "api_key": "user-api-key",
  "device_id": "device-001",
  "device_name": "Windows 主机"
}
```

### 解密后成功响应

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "auth_mode": "api_key",
    "role": 0,
    "config": {},
    "device_id": "device-001",
    "device_name": "Windows 主机",
    "t": 1780000000
  }
}
```

业务规则：

- 用户不存在时返回 `-1001`。
- 新设备数量达到 `max_devices` 时返回 `-1002`。
- 已绑定设备会更新最后访问时间。
- 非空 `device_name` 会更新设备名称。

## 5. 获取当前比赛配置

`POST /v2/matches/current/config`

### 解密后请求参数

```json
{
  "api_key": "user-api-key"
}
```

### 解密后成功响应

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "info": {
      "id": 86,
      "team1": "成都蓉城",
      "team2": "对手球队",
      "start": 1782302400,
      "sale_start": 1782108000
    },
    "notice": "比赛公告"
  }
}
```

可能的业务错误：

- `-1001`：用户不存在。
- `-11`：当前比赛文件不存在。
- `-13`：当前比赛配置不可用。

## 6. 获取比赛详情

`POST /v2/matches/detail`

### 解密后请求参数

| 字段 | 类型 | 必填 | 约束 |
|---|---|---:|---|
| `api_key` | string | 是 | 1-64 字符 |
| `match_id` | integer | 是 | 大于 0 |

```json
{
  "api_key": "user-api-key",
  "match_id": 86
}
```

### 解密后成功响应

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "match_id": 86,
    "detail": {},
    "t": 1780000000
  }
}
```

可能的业务错误：

- `-1001`：用户不存在。
- `-2001`：外部比赛服务不可用。

## 7. 任务校验

`POST /v2/tasks/check`

### 解密后请求参数

| 字段 | 类型 | 必填 | 约束 |
|---|---|---:|---|
| `api_key` | string | 是 | 1-64 字符 |
| `device_id` | string | 是 | 1-128 字符 |
| `device_name` | string/null | 否 | 最长 128 字符 |

```json
{
  "api_key": "user-api-key",
  "device_id": "device-001",
  "device_name": "Windows 主机"
}
```

### 解密后成功响应

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "device_id": "device-001",
    "device_name": "Windows 主机",
    "t": 1780000000
  }
}
```

可能的业务错误：

- `-1001`：用户不存在。
- `-1003`：设备未绑定。

## 8. 订单上报

`POST /v2/orders`

成功 HTTP 状态码为 `201`。

### 8.1 解密后请求参数

| 字段 | 类型 | 必填 | 默认值/约束 | 说明 |
|---|---|---:|---|---|
| `api_key` | string | 是 | 1-64 字符 | 用户密钥 |
| `task_id` | string | 是 | 1-128 字符 | 客户端对应任务 ID；数值会转换为字符串 |
| `device_id` | string | 是 | 1-128 字符 | 客户端设备标识 |
| `device_name` | string/null | 否 | 最长 128 字符 | 设备名称快照 |
| `order_ip` | string | 是 | 合法 IPv4/IPv6，最长 45 字符 | 下单使用的 IP，不带端口 |
| `match_id` | integer/null | 否 | 大于 0 | 比赛 ID |
| `ticket_count` | integer/null | 否 | 大于等于 0 | 省略时取 `ticket_holders` 数量 |
| `ticket_holders` | array | 否 | 默认 `[]`，最多 20 项 | 购票人数组；兼容别名 `tickets` |
| `first_delay` | integer | 否 | 默认 `0`，大于等于 0 | 首开延迟 |
| `task_type` | integer | 否 | 默认 `0`，范围 0-3 | 任务类型；兼容别名 `type` |

V2 不接收 `first_start_t` 和 `first_end_t`。传入这两个字段会返回加密的 `422/-422`。

### 8.2 购票人对象

| 字段 | 类型 | 必填 | 约束 |
|---|---|---:|---|
| `name` | string | 是 | 1-128 字符 |
| `card` | string/null | 否 | 最长 128 字符 |
| `phone` | string/null | 否 | 最长 128 字符 |
| `region` | string/null | 否 | 最长 128 字符 |
| `price` | string/null | 否 | 最长 128 字符 |

购票人字段中的数值标量会转换为字符串。

任务类型：

| `task_type` | 含义 |
|---:|---|
| `0` | 首开 |
| `1` | 捡漏 |
| `2` | 广播 |
| `3` | 蹲坑 |

### 8.3 完整请求示例

以下 JSON 是加密前的业务参数：

```json
{
  "api_key": "user-api-key",
  "task_id": "task-20260802-001",
  "device_id": "device-001",
  "device_name": "Windows 主机",
  "order_ip": "203.0.113.10",
  "match_id": 86,
  "ticket_count": 2,
  "ticket_holders": [
    {
      "name": "张三",
      "card": "510000000000000001",
      "phone": "13800000000",
      "region": "A区",
      "price": "280"
    },
    {
      "name": "李四",
      "card": "510000000000000002",
      "phone": "13900000000",
      "region": "A区",
      "price": "280"
    }
  ],
  "first_delay": 0,
  "task_type": 0
}
```

实际 HTTP 请求体：

```json
{
  "payload": "上述 JSON 加密后的 Base64 字符串"
}
```

### 8.4 解密后成功响应

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "order_id": 123,
    "task_id": "task-20260802-001",
    "parse_status": "ok",
    "t": 1780000000
  }
}
```

业务规则：

- `api_key` 必须存在。
- 订单上报不要求 `device_id` 已经绑定；已绑定时会更新设备最后访问时间和设备名称。
- 服务端保存解密后的原始业务 JSON、结构化购票人数据和客户端任务 ID。
- 数据库保存失败时返回 `-5001`。

## 9. 客户端错误码

| HTTP 状态 | `code` | 含义 | 响应是否加密 |
|---:|---:|---|---:|
| `400` | `-4001` | 缺少外壳或密文无法解密 | 否 |
| `401` | `-1001` | 用户不存在 | 是 |
| `404` | `-11` | 当前比赛文件不存在 | 是 |
| `404` | `-1003` | 设备未绑定 | 是 |
| `409` | `-1002` | 设备数量达到上限 | 是 |
| `422` | `-422` | 解密后参数校验失败 | 是 |
| `500` | `-500` | 服务端内部错误 | 是 |
| `500` | `-5001` | 订单保存失败 | 是 |
| `502` | `-2001` | 外部比赛服务不可用 | 是 |
| `503` | `-13` | 当前比赛配置不可用 | 是 |

## 10. 管理员 API 约定

管理员接口前缀为 `/v2/admin`，不使用 AES-CBC 外壳，使用普通 JSON。

所有请求必须携带：

```http
Authorization: Bearer <ADMIN_API_TOKEN>
```

服务端优先读取 `ADMIN_API_TOKEN`；如果未设置，为兼容旧部署会临时回退使用 `ADMIN_PASSWORD`。

管理员成功响应：

```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

管理员错误响应同样是普通 JSON，并使用对应 HTTP 状态码。

## 11. 管理员接口

### 11.1 创建用户

`POST /v2/admin/users`

成功状态码：`201`

```json
{
  "name": "测试用户",
  "api_key": "user-api-key",
  "lark_key": null,
  "max_devices": 2,
  "role": 0
}
```

| 字段 | 必填 | 约束 |
|---|---:|---|
| `name` | 是 | 1-32 字符 |
| `api_key` | 是 | 1-64 字符，不能重复 |
| `lark_key` | 否 | 最长 128 字符 |
| `max_devices` | 否 | 正整数，默认 1 |
| `role` | 否 | `0` 或 `1`，默认 0 |

`api_key` 已存在时返回 HTTP `409`。

### 11.2 用户列表

`GET /v2/admin/users`

返回全部用户，按内部用户 ID 升序排列。

### 11.3 用户详情

`GET /v2/admin/users/{user_id}`

`user_id` 必须为正整数。用户不存在时返回 HTTP `404`。

### 11.4 更新用户

`PATCH /v2/admin/users/{user_id}`

请求体可以包含以下任意字段：

```json
{
  "name": "新名称",
  "lark_key": null,
  "max_devices": 3,
  "role": 1
}
```

`lark_key: null` 可以清空飞书 Key。`max_devices` 必须为正整数，`role` 仅支持 `0` 或 `1`。

### 11.5 删除用户

`DELETE /v2/admin/users/{user_id}`

成功响应：

```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

### 11.6 用户配置

查询配置：

```text
GET /v2/admin/users/{user_id}/config
```

替换配置：

```text
PUT /v2/admin/users/{user_id}/config
```

PUT 请求体：

```json
{
  "config": {
    "wechat_port": "8062",
    "platform": "windows"
  }
}
```

`config` 必须为 JSON 对象或 `null`。传入 `null` 时保存为空对象。

### 11.7 用户设备

`GET /v2/admin/users/{user_id}/devices`

返回设备 ID、设备名称、首次访问时间和最后访问时间。

### 11.8 出票总数

`GET /v2/admin/users/{user_id}/tickets-count`

成功响应示例：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "user_id": 1,
    "tickets_count": 10
  }
}
```

### 11.9 设置比赛公告

`PUT /v2/admin/matches/{match_id}/notice`

```json
{
  "content": "比赛公告内容"
}
```

`match_id` 必须为正整数，`content` 最长 1024 字符。不存在时创建，存在时替换。

### 11.10 订单列表

`GET /v2/admin/orders`

查询参数：

| 参数 | 类型 | 必填 | 约束/说明 |
|---|---|---:|---|
| `user_id` | integer | 否 | 正整数 |
| `task_id` | string | 否 | 1-128 字符，精确匹配 |
| `device_id` | string | 否 | 1-128 字符，精确匹配 |
| `device_name` | string | 否 | 1-128 字符，模糊匹配 |
| `order_ip` | string | 否 | 1-45 字符，精确匹配 |
| `match_id` | integer | 否 | 正整数 |
| `parse_status` | string | 否 | `ok`、`partial`、`error`、`unknown_key` |
| `page` | integer | 否 | 默认 1，最小 1 |
| `page_size` | integer | 否 | 默认 50，范围 1-200 |

示例：

```text
GET /v2/admin/orders?task_id=task-20260802-001&order_ip=203.0.113.10&page=1&page_size=50
```

成功响应：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "page": 1,
    "page_size": 50,
    "total": 1,
    "orders": [
      {
        "id": 123,
        "user_id": 1,
        "api_key": "user-api-key",
        "task_id": "task-20260802-001",
        "raw_api_key": "user-api-key",
        "device_id": "device-001",
        "device_name": "Windows 主机",
        "order_ip": "203.0.113.10",
        "match_id": 86,
        "ticket_count": 2,
        "ticket_holders": [],
        "first_delay": 0,
        "task_type": 0,
        "parse_status": "ok",
        "parse_error": null,
        "raw_payload": "{...}",
        "created_at": 1780000000
      }
    ]
  }
}
```

## 12. 管理控制台

当前管理控制台不是独立的 V2 页面，继续使用：

```text
/admin-ui/login
```

本地地址：

```text
http://127.0.0.1:8000/admin-ui/login
```

控制台使用 `ADMIN_PASSWORD` 登录并写入 Session。订单管理页面会显示设备名和任务 ID、下单 IP、下单开始时间和服务响应时间。

## 13. 环境变量

| 变量 | 用途 |
|---|---|
| `DATABASE_URL` | SQLAlchemy 数据库连接串 |
| `AES_KEY` | 客户端接口 AES Key，16/24/32 字节 |
| `AES_IV` | 客户端接口 AES IV，16 字节 |
| `ADMIN_API_TOKEN` | V2 管理员 API Bearer Token |
| `ADMIN_PASSWORD` | 管理控制台密码；管理员 API Token 未配置时的兼容回退值 |
| `ADMIN_UI_SESSION_SECRET` | 管理控制台 Session 密钥 |
| `MATCH_API_BASE_URL` | 外部比赛详情接口地址 |
| `MATCH_API_LID2` | 外部比赛接口参数 |
| `MATCH_API_BEARER` | 外部比赛接口 Bearer Token |
| `REDIS_URL` | 公开查询页面限流 Redis 地址 |
| `QUERY_COOLDOWN_SECONDS` | 公开查询冷却秒数 |
| `LOG_LEVEL` | 日志等级 |

## 14. 接入检查清单

- 客户端请求外层只有 `payload`。
- 解密后的业务参数是 JSON 对象。
- 客户端可以解密成功响应和业务错误响应。
- 客户端对明文 `-4001` 做单独处理。
- 客户端订单上报包含必填的 `task_id`、`device_id` 和 `order_ip`。
- `order_ip` 不包含端口。
- V2 订单不上传 `first_start_t` 或 `first_end_t`。
- 管理员 API 使用 `ADMIN_API_TOKEN`，不把密码放入 Query 参数。
