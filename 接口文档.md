# 票务服务端接口文档

> 第 1 至 7 节为兼容保留的 V1 接口；新客户端请使用独立文档 `V2接口文档.md`。

## 1. 基本约定

### 1.1 通用返回

客户端加密接口的正常响应外壳为：

```json
{
  "payload": "AES加密后的Base64字符串"
}
```

解密后得到完整响应对象：

```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

### 1.2 加密请求

客户端接口请求统一使用：

```json
{
  "payload": "AES加密后的Base64字符串"
}
```

`payload` 解密后必须是 JSON 对象。

### 1.3 加密算法

- AES-CBC
- `AES_KEY` 取环境变量 `AES_KEY`
- `AES_IV` 取环境变量 `AES_IV`
- 明文使用 UTF-8
- 密文使用 Base64

### 1.4 明文错误

当请求缺少 `payload` 或解密失败时，服务端可返回明文：

```json
{
  "code": -4001,
  "data": null,
  "msg": "invalid encrypted request"
}
```

### 1.5 客户端错误码

客户端加密接口统一使用外层 `code` 判断结果：

| code | 含义 |
|---|---|
| `0` | 成功 |
| `-4001` | 加密请求无效 |
| `-1001` | 用户不存在 |
| `-1002` | 设备数量超限 |
| `-1003` | 设备未绑定 |
| `-2001` | 外部比赛接口异常 |
| `-5001` | 订单保存失败 |

`data` 只承载业务数据，不再放 `status` 或 `desc`。

---

## 2. 客户端接口

### 2.1 授权

`POST /auth`

请求解密后：

```json
{
  "api_key": "string",
  "device_id": "string",
  "device_name": "string"
}
```

响应解密后：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "api_key": "string",
    "lark_key": "string",
    "role": 0,
    "config": {
      "wechat_port": "8062",
      "forbid_cards": ["5001", "5002", "5102"],
      "user_agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/132.0.0.0 Safari/537.36 MicroMessenger/7.0.20.1781(0x6700143B) NetType/WIFI MiniProgramEnv/Windows WindowsWechat/WMPF WindowsWechat(0x63090a13) UnifiedPCWindowsWechat(0xf2541212) XWEB/16815",
      "platform": "windows"
    },
    "device_id": "string",
    "device_name": "string",
    "t": 1780000000
  }
}
```

字段说明：

- `role`：用户权限，`0` 表示普通用户，`1` 表示管理员。
- `config`：当前 `api_key` 对应的 JSON 配置；未配置时返回 `{}`。

失败时看外层 `code/msg`：

- `-1001`：用户不存在
- `-1002`：设备数量超限

---

### 2.2 获取比赛配置

`POST /match/config`

请求解密后：

```json
{}
```

响应解密后：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "info": {
      "id": 86,
      "team1": "成都蓉城希拉谷",
      "team2": "广州蒲公英",
      "start": 1782302400,
      "sale_start": 1782108000
    },
    "notice": "公告内容"
  }
}
```

---

### 2.3 查询比赛详情

`POST /match/detail`

请求解密后：

```json
{
  "api_key": "string",
  "match_id": "string"
}
```

响应解密后：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "detail": {},
    "t": 1780000000
  }
}
```

失败时看外层 `code/msg`：

- `-1001`：用户不存在
- `-2001`：外部比赛接口异常

---

### 2.4 任务校验

`POST /task/check`

请求解密后：

```json
{
  "api_key": "string",
  "device_id": "string"
}
```

响应解密后：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "api_key": "string",
    "device_id": "string",
    "device_name": "string",
    "t": 1780000000
  }
}
```

失败时看外层 `code/msg`：

- `-1001`：用户不存在
- `-1003`：设备未绑定

---

### 2.5 订单上报

`POST /task/order`

请求解密后示例：

```json
{
  "api_key": "string",
  "device_id": "string",
  "match_id": 86,
  "ticket_count": 2,
  "order_names": "张三&李四",
  "order_cards": "string",
  "order_phones": "string",
  "order_region": "string",
  "order_price": "string",
  "first_delay": 0,
  "first_start_t": "string",
  "first_end_t": "string",
  "type": 0
}
```

业务原则：

- 不要求设备已授权
- 不要求 `api_key` 一定存在
- 不要求字段完整
- 解密成功后尽量入库
- 保存原始 `raw_payload`
- 保存 `raw_api_key`
- 保存服务端已绑定设备中的 `device_name` 快照
- 记录 `parse_status`
- 记录 `parse_error`

响应解密后：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "order_id": 123,
    "parse_status": "ok",
    "t": 1780000000
  }
}
```

部分解析成功时：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "order_id": 123,
    "parse_status": "partial",
    "parse_error": "missing field: order_cards",
    "t": 1780000000
  }
}
```

状态说明：

- `ok`：完整解析
- `partial`：部分字段缺失或类型异常
- `unknown_key`：`api_key` 不存在

---

## 3. 数据模型

### 3.1 users

| 字段 | 说明 |
|---|---|
| `id` | 主键 |
| `name` | 用户名称 |
| `api_key` | 用户密钥，唯一 |
| `lark_key` | 飞书 key |
| `max_devices` | 最大设备数 |
| `role` | 用户权限，`0` 普通用户，`1` 管理员 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

### 3.2 devices

| 字段 | 说明 |
|---|---|
| `id` | 主键 |
| `user_id` | 用户 ID |
| `device_id` | 设备 ID |
| `device_name` | 设备名 |
| `first_seen` | 首次访问时间 |
| `last_seen` | 最后访问时间 |

### 3.3 orders

| 字段 | 说明 |
|---|---|
| `id` | 主键 |
| `user_id` | 用户 ID，可空 |
| `raw_api_key` | 原始 `api_key` |
| `device_id` | 设备 ID |
| `device_name` | 设备名快照 |
| `order_ip` | 下单使用的 IPv4/IPv6 地址，V2 上报 |
| `match_id` | 比赛 ID |
| `ticket_count` | 票数 |
| `order_names` | 姓名 |
| `order_cards` | 证件号 |
| `order_phones` | 手机号 |
| `order_region` | 区域 |
| `order_price` | 价格 |
| `first_delay` | 首开延迟 |
| `first_start_t` | 首开开始时间 |
| `first_end_t` | 首开结束时间 |
| `type` | 任务类型 |
| `raw_payload` | 解密后的完整原始 payload |
| `parse_status` | 解析状态 |
| `parse_error` | 解析错误 |
| `created_at` | 创建时间 |

### 3.4 configs

| 字段 | 说明 |
|---|---|
| `id` | 主键 |
| `match_id` | 比赛 ID，唯一 |
| `content` | 公告内容 |

### 3.5 user_configs

| 字段 | 说明 |
|---|---|
| `id` | 主键 |
| `user_id` | 用户 ID，唯一关联 users |
| `config` | 当前 `api_key` 对应的 JSON 配置文本 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

---

## 4. 管理员 API

管理员 API 不使用客户端加密协议，使用普通参数。

### 4.1 创建或更新比赛公告

`POST /admin/match/config`

参数：

- `password`
- `content`
- `match_id`

---

### 4.2 创建用户

`POST /admin/user/create`

参数：

- `password`
- `name`
- `api_key`
- `lark_key`
- `max_devices`
- `role`：可选，`0` 普通用户，`1` 管理员，默认 `0`

---

### 4.3 删除用户

`DELETE /admin/user/delete`

参数：

- `password`
- `api_key`

---

### 4.4 更新用户

`PUT /admin/user/update`

参数：

- `password`
- `api_key`
- `name`
- `lark_key`
- `max_devices`
- `role`：可选，`0` 普通用户，`1` 管理员

---

### 4.5 查询用户列表

`GET /admin/user/list`

参数：

- `password`

---

### 4.6 查询 / 保存 api_key 配置

`GET /admin/user/config`

参数：

- `password`
- `api_key`

返回：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "api_key": "string",
    "config": {
      "example": true
    },
    "updated_at": 1780000000
  }
}
```

`POST /admin/user/config`

参数：

- `password`
- `api_key`
- `config`：JSON 字符串，例如 `{"example": true}`；为空表示无配置

---

### 4.7 查询用户设备

`GET /admin/user/devices`

参数：

- `password`
- `api_key`

---

### 4.8 查询出票总数

`GET /admin/task/tickets_count`

参数：

- `password`
- `api_key`

---

### 4.9 查询订单列表

`GET /admin/orders/list`

参数：

- `password`
- `api_key`
- `raw_api_key`
- `device_id`
- `device_name`
- `match_id`
- `parse_status`
- `page`
- `page_size`

---

## 5. 后台页面

### 5.1 登录

`GET /admin-ui/login`

`POST /admin-ui/login`

使用 `ADMIN_PASSWORD` 登录，成功后写入 Session。

访问 `/admin-ui` 或登录后未指定跳转地址时，默认进入实时任务页面 `/admin-ui/live-tasks`。

### 5.2 用户管理

`GET /admin-ui/users`

功能：

- 创建 / 更新用户密钥
- 设置用户权限：普通用户或管理员
- 在用户密钥页面内维护该 `api_key` 对应的 JSON 配置
- 查看配置摘要

### 5.3 用户设备

`GET /admin-ui/users/{api_key}/devices`

### 5.4 订单管理

`GET /admin-ui/orders`

支持筛选：

- `api_key`
- `raw_api_key`
- `device_id`
- `device_name`
- `match_id`
- `parse_status`

### 5.5 公告配置

`GET /admin-ui/configs`

---

## 6. 公开查询页面

### 6.1 查询页

`GET /query`

### 6.2 提交查询

`POST /query`

参数：

- `name`

规则：

- 以当前 `match.json` 的比赛 ID 为查询范围
- `order_names` 按 `&` 拆分
- 完全匹配姓名
- 同一 IP 30 秒内只能查询一次
- 使用 Redis 限流
- Redis 异常时降级为允许查询

返回结果展示：

- 姓名
- 区域

---

## 7. 环境变量

| 变量 | 说明 |
|---|---|
| `DATABASE_URL` | MySQL 连接串 |
| `AES_KEY` | AES 密钥 |
| `AES_IV` | AES IV |
| `ADMIN_PASSWORD` | 后台密码 |
| `ADMIN_API_TOKEN` | V2 管理员 API Bearer Token |
| `ADMIN_UI_SESSION_SECRET` | 后台 Session 密钥 |
| `MATCH_API_BASE_URL` | 外部比赛接口地址 |
| `MATCH_API_LID2` | 外部接口参数 |
| `MATCH_API_BEARER` | 外部接口 Token |
| `REDIS_URL` | 公开查询限流 Redis 地址 |
| `QUERY_COOLDOWN_SECONDS` | 查询冷却秒数，默认 30 |
| `LOG_LEVEL` | 日志等级 |

---

## 8. V2 API

V2 客户端接口继续使用 AES-CBC 整体加密。HTTPS 可以同时保留作为传输层保护，但不能替代应用层 `payload` 加密。

所有客户端请求外层统一为：

```json
{
  "payload": "AES-CBC 加密后的 Base64 字符串"
}
```

`api_key`、`match_id`、`task_id` 等业务参数全部放在 `payload` 解密后的 JSON 中，不通过 Header、Path 或 Query 传递。成功响应和业务错误响应同样使用加密 `payload`。只有外壳缺失或无法解密时返回明文 `-4001`。

V2 管理员 API 仍使用独立的 Bearer Token。生产环境必须配置 `ADMIN_API_TOKEN`；未配置时服务端仅为兼容部署临时回退使用 `ADMIN_PASSWORD`，建议立即设置独立的随机 Token。

### 8.1 客户端授权

`POST /v2/auth`

`payload` 解密后：

```json
{
  "api_key": "string",
  "device_id": "string",
  "device_name": "string"
}
```

需要用户鉴权的后续客户端请求仍在各自的加密业务参数中携带 `api_key`，不使用 `Authorization` Header。当前比赛配置接口无需用户鉴权。

### 8.2 比赛与任务

- `POST /v2/matches/current/config`：解密后参数可以为 `{}`；兼容接收 `api_key`，但不校验用户是否存在。
- `POST /v2/matches/detail`：解密后参数为 `api_key` 和 `match_id`。
- `POST /v2/tasks/check`：解密后参数为 `api_key`、`device_id` 和可选的 `device_name`。
- `POST /v2/tasks/report`：解密后参数为 `api_key`、`device_id`、字符串 `task_id`、`action`（`add`/`delete`）和 `status`（`online`/`offline`）；任务按 API Key 和设备分别保存。

`match_id` 统一为正整数。

### 8.3 V2 订单上报

`POST /v2/orders`

`payload` 解密后的订单参数：

```json
{
  "api_key": "string",
  "task_id": "client-task-20260801-0001",
  "device_id": "device-001",
  "device_name": "Windows 主机",
  "order_ip": "203.0.113.10",
  "match_id": 86,
  "ticket_count": 2,
  "ticket_holders": [
    {
      "name": "张三",
      "phone": "手机号",
      "region": "A区",
      "price": "280"
    }
  ],
  "first_delay": 0,
  "task_type": 0
}
```

规则：

- `task_id` 必填，长度 1 至 128，用于关联客户端任务。
- `order_ip` 必填，必须是不带端口的合法 IPv4 或 IPv6 地址。
- `first_start_t` 和 `first_end_t` 不属于 V2 请求参数，传入会返回 `422`。
- V2 不采集身份证或其他证件号码，购票人对象传入 `card` 会返回 `422`。
- `ticket_holders` 使用结构化数组，不再使用 `&` 拼接多个平行字符串。
- `ticket_count` 省略时按 `ticket_holders` 数量计算。
- `task_type` 仅支持 `0`、`1`、`2`、`3`；为兼容客户端也接受请求字段 `type`。
- 订单成功写入后返回 `201`，响应外层仍为 `payload`；解密后为：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "order_id": 123,
    "task_id": "client-task-20260801-0001",
    "parse_status": "ok",
    "t": 1780000000
  }
}
```

### 8.4 V2 管理员 API

所有接口均要求 `Authorization: Bearer <ADMIN_API_TOKEN>`，不再传 `password`：

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/v2/admin/users` | 创建用户，已存在的 `api_key` 返回 `409` |
| `GET` | `/v2/admin/users` | 用户列表 |
| `GET` | `/v2/admin/users/{user_id}` | 用户详情 |
| `PATCH` | `/v2/admin/users/{user_id}` | 更新用户 |
| `DELETE` | `/v2/admin/users/{user_id}` | 删除用户 |
| `GET/PUT` | `/v2/admin/users/{user_id}/config` | 查询或替换 JSON 配置 |
| `GET` | `/v2/admin/users/{user_id}/devices` | 查询设备 |
| `GET` | `/v2/admin/users/{user_id}/tickets-count` | 查询出票总数 |
| `PUT` | `/v2/admin/matches/{match_id}/notice` | 创建或替换比赛公告 |
| `GET` | `/v2/admin/orders` | 订单筛选和分页 |

V2 管理员订单支持 `task_id`、`user_id`、`device_id`、`device_name`、`order_ip`、`match_id`、`parse_status`、`page` 和 `page_size` 查询参数。作为凭证使用的 `api_key` 不放入 URL。
