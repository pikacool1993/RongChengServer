# 票务服务端重构版需求文档

## 1. 项目概述

本项目是一个基于 FastAPI 的票务任务管理与结果查询服务端。重构后系统以“请求/响应全量 AES 加密”和“订单收到即保存”为核心原则，主要服务于客户端授权、比赛配置获取、比赛详情查询、订单上报、后台管理和公开结果查询。

本次重构不要求兼容旧接口，客户端将根据新版接口文档重新调整。

---

## 2. 重构核心目标

1. **不兼容旧接口**：直接采用新版接口协议和新版数据结构。
2. **请求参数整体 AES 加密**：客户端请求体不再直接传业务 JSON，而是传加密后的 `payload`。
3. **响应内容整体 AES 加密**：服务端不再只加密 `data`，而是将完整响应对象加密后返回。
4. **订单收到即保存**：订单上报只要请求成功解密并能得到可保存内容，即尽量入库。
5. **不存在的 key 也入库**：`api_key` 不存在时不拒绝订单，订单仍然保存，并记录原始 `raw_api_key`。
6. **解析异常订单也入库**：字段缺失、字段类型异常、结构不完整时，仍保存原始 payload，并记录解析状态和解析错误。
7. **服务端少校验**：订单上报不因设备未授权、key 不存在、非关键字段异常而拒绝入库。
8. **设备增加设备名**：设备表增加 `device_name`，后台设备和订单页面应展示设备名。

---

## 3. 技术栈

| 类型 | 技术 |
|---|---|
| Web 框架 | FastAPI |
| ASGI 服务 | Uvicorn |
| ORM | SQLAlchemy |
| 数据库 | MySQL 8 |
| 数据库驱动 | PyMySQL |
| 模板引擎 | Jinja2 |
| HTTP 客户端 | httpx |
| 加密 | AES，优先沿用当前 AES-CBC 方案 |
| 配置 | python-dotenv / 环境变量 |
| 后台 Session | Starlette SessionMiddleware |

---

## 4. 系统角色

### 4.1 客户端

客户端负责：

- 生成业务请求 JSON。
- 使用 AES 加密请求 JSON。
- 调用新版服务端接口。
- 解密服务端响应。
- 上报订单时只上报一次，不设计客户端重试机制。

### 4.2 管理员

管理员负责：

- 管理用户 key。
- 管理设备。
- 查看订单。
- 查看异常订单。
- 管理比赛公告。
- 查看统计结果。

### 4.3 普通查询用户

普通查询用户通过 `/query` 页面按姓名查询当前比赛出票结果。

---

## 5. 新版加密协议

## 5.1 请求格式

所有客户端 API 请求使用统一加密外壳：

```json
{
  "payload": "AES加密后的Base64字符串"
}
```

`payload` 解密后是业务 JSON。

示例：

```json
{
  "api_key": "test-key",
  "device_id": "device-001",
  "device_name": "张三的电脑"
}
```

## 5.2 响应格式

所有客户端 API 响应使用统一加密外壳：

```json
{
  "payload": "AES加密后的Base64字符串"
}
```

`payload` 解密后是完整响应对象：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "status": 1,
    "desc": "ok"
  }
}
```

## 5.3 无法解密时的响应

如果请求缺少 `payload`、`payload` 非法、AES 解密失败或解密后不是合法 JSON，服务端无法生成正常加密响应时，可返回最小明文错误：

```json
{
  "code": -4001,
  "data": null,
  "msg": "invalid encrypted request"
}
```

## 5.4 是否保留原签名

新版接口不再依赖旧版 `Timestamp` + `Sign` 请求头签名机制。请求内容通过 AES 加密传输。后续如需增强防篡改，可升级为 AES-GCM 或增加 HMAC。

---

## 6. 订单上报核心规则

订单上报是本系统最重要的链路，重构后采用“收到即保存、少校验、保留原始数据”的原则。

## 6.1 入库原则

只要满足以下条件，订单就应尽量入库：

1. 请求存在 `payload`。
2. 服务端能成功 AES 解密。
3. 解密内容能解析为 JSON 或可保存的原始内容。

以下情况不应阻止订单入库：

1. `api_key` 不存在。
2. `device_id` 未授权。
3. 设备未绑定。
4. 非关键字段缺失。
5. 字段类型异常但仍可转换或可记录。
6. 订单结构不完整。

以下情况可以导致订单无法入库：

1. 请求没有到达服务端。
2. AES 解密失败。
3. 解密内容完全无法作为 JSON 或原始内容保存。
4. 数据库不可用或写入失败。

## 6.2 不存在的 key 处理

当订单上报中的 `api_key` 不存在时：

1. 不返回用户不存在导致订单丢弃。
2. 订单仍然入库。
3. `user_id` 可以为空。
4. 原始 key 保存到 `raw_api_key`。
5. 解析状态可标记为 `unknown_key` 或 `partial`。

## 6.3 解析异常处理

当订单字段缺失或格式异常时：

1. 能提取的字段正常保存。
2. 不能提取的字段保存为空或默认值。
3. 完整原始 payload 保存到 `raw_payload`。
4. `parse_status` 记录为 `partial` 或 `error`。
5. `parse_error` 记录字段缺失或解析异常原因。
6. 后台应能查看这类异常订单。

## 6.4 不做重试和幂等

本系统不设计客户端重试机制，客户端订单只上报一次。服务端也不强制要求 `client_order_id` 幂等字段。

服务端能力边界为：

> 只要请求成功到达服务端、成功解密，并且数据库可写，服务端应尽量保存订单。客户端未发出、网络中断导致请求未到达、数据库不可用等情况不在服务端防漏单能力范围内。

---

## 7. 数据库设计

## 7.1 users 表

用户 key 表。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | Integer | 主键 |
| `name` | String(32) | 用户名称 |
| `api_key` | String(64) | 用户密钥，唯一 |
| `max_devices` | Integer | 最大绑定设备数量 |
| `created_at` | DateTime | 创建时间 |
| `updated_at` | DateTime | 更新时间 |

## 7.2 devices 表

设备绑定表。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | Integer | 主键 |
| `user_id` | Integer | 用户 ID，可关联 users |
| `device_id` | String(128) | 客户端设备 ID |
| `device_name` | String(128) | 设备名，可为空 |
| `first_seen` | DateTime | 首次访问时间 |
| `last_seen` | DateTime | 最后访问时间 |

设备名规则：

1. 客户端授权、任务校验或订单上报时可传 `device_name`。
2. 首次绑定设备时保存 `device_name`。
3. 设备已存在且本次传入非空 `device_name` 时，更新设备名。
4. 后台设备列表和订单列表应展示设备名。

## 7.3 orders 表

订单记录表。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | Integer | 主键 |
| `user_id` | Integer nullable | 用户 ID，key 不存在时可为空 |
| `raw_api_key` | String(128) nullable | 客户端原始 api_key |
| `device_id` | String(128) nullable | 客户端设备 ID |
| `device_name` | String(128) nullable | 设备名快照 |
| `match_id` | Integer nullable | 比赛 ID，解析不到可为空 |
| `ticket_count` | Integer | 票数，默认 0 |
| `order_names` | String(128) nullable | 购票人姓名 |
| `order_cards` | String(128) nullable | 证件号 |
| `order_phones` | String(128) nullable | 手机号 |
| `order_region` | String(128) nullable | 区域 |
| `order_price` | String(128) nullable | 价格 |
| `first_delay` | Integer | 首开延迟，默认 0 |
| `first_start_t` | String(128) nullable | 首开开始时间 |
| `first_end_t` | String(128) nullable | 首开结束时间 |
| `type` | Integer | 任务类型，默认 0 |
| `raw_payload` | Text | 解密后的完整原始 payload |
| `parse_status` | String(32) | 解析状态：`ok`、`partial`、`error`、`unknown_key` |
| `parse_error` | Text nullable | 解析异常说明 |
| `customer_notified` | Boolean | 是否已人工通知客户，默认 `false` |
| `created_at` | DateTime | 创建时间 |

任务类型：

| type | 含义 |
|---|---|
| 0 | 首开 |
| 1 | 捡漏 |
| 2 | 广播 |
| 3 | 蹲坑 |

## 7.4 configs 表

比赛公告配置表。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | Integer | 主键 |
| `match_id` | Integer | 比赛 ID，唯一 |
| `content` | String(1024) | 公告内容 |

---

## 8. 客户端 API

所有客户端接口均使用加密请求和加密响应。

## 8.1 授权接口

`POST /auth`

请求外层：

```json
{
  "payload": "..."
}
```

payload 解密后：

```json
{
  "api_key": "string",
  "device_id": "string",
  "device_name": "string"
}
```

业务规则：

1. `api_key` 存在时，按用户设备数量规则进行设备绑定。
2. 设备已存在时，更新 `last_seen`。
3. 如果传入 `device_name`，保存或更新设备名。
4. `api_key` 不存在时，授权接口仍可返回用户不存在。

响应 payload 解密后：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "status": 1,
    "desc": "ok",
    "t": 1780000000
  }
}
```

## 8.2 获取比赛配置

`POST /match/config`

> 新版可统一使用 POST 加密请求，即使无业务参数也传空 payload。

payload 解密后：

```json
{}
```

响应 payload 解密后：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "info": {
      "id": 86,
      "team1": "成都蓉城希拉谷",
      "team2": "广州蒲公英",
      "start": 1782302400,
      "sale_start": 1782108000
    },
    "notice": "公告内容"
  }
}
```

## 8.3 查询比赛详情

`POST /match/detail`

payload 解密后：

```json
{
  "api_key": "string",
  "match_id": "string"
}
```

业务规则：

1. `api_key` 存在时调用外部接口查询比赛详情。
2. `api_key` 不存在时可返回用户不存在。
3. 外部接口异常时返回加密后的异常响应。

## 8.4 任务校验

`POST /task/check`

payload 解密后：

```json
{
  "api_key": "string",
  "device_id": "string",
  "device_name": "string"
}
```

业务规则：

1. `api_key` 不存在，返回用户不存在。
2. `device_id` 未绑定，返回拒绝。
3. 设备存在时更新 `last_seen`。
4. 如果传入 `device_name`，更新设备名。

## 8.5 订单上报

`POST /task/order`

payload 解密后示例：

```json
{
  "api_key": "string",
  "device_id": "string",
  "device_name": "string",
  "match_id": 86,
  "ticket_count": 2,
  "order_names": "张三&李四",
  "order_cards": "string",
  "order_phones": "string",
  "order_region": "string",
  "order_price": "string",
  "first_delay": 0,
  "first_start_t": "string",
  "first_end_t": "string",
  "type": 0
}
```

订单上报规则：

1. 不要求设备已授权。
2. 不要求 `api_key` 一定存在。
3. 不要求所有字段完整。
4. 解密成功后优先保存原始 payload。
5. 能解析出的字段写入对应字段。
6. 不能解析的字段记录到 `parse_error`。
7. 数据库提交成功后返回成功。

成功响应 payload 解密后：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "status": 1,
    "desc": "saved",
    "order_id": 123,
    "parse_status": "ok",
    "t": 1780000000
  }
}
```

部分解析成功响应示例：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "status": 1,
    "desc": "saved with parse warning",
    "order_id": 123,
    "parse_status": "partial",
    "parse_error": "missing field: order_cards",
    "t": 1780000000
  }
}
```

---

## 9. 管理后台需求

后台页面继续使用浏览器表单和 Session 登录，不强制使用客户端 AES 协议。

## 9.1 登录

路径：`/admin-ui/login`

规则：

1. 使用 `ADMIN_PASSWORD` 登录。
2. 登录成功后写入 Session。
3. Session 使用 `ADMIN_UI_SESSION_SECRET` 签名。

## 9.2 用户管理

路径：`/admin-ui/users`

功能：

1. 创建用户。
2. 更新用户。
3. 删除用户。
4. 查看用户列表。
5. 查看用户绑定设备。

## 9.3 设备管理

路径：`/admin-ui/users/{api_key}/devices`

功能：

1. 查看设备 ID。
2. 查看设备名 `device_name`。
3. 查看首次访问时间。
4. 查看最后访问时间。
5. 删除设备绑定。

## 9.4 订单管理

路径：`/admin-ui/orders`

功能：

1. 查看全部原始订单。
2. 查看正常订单。
3. 查看异常订单。
4. 查看 unknown key 订单。
5. 按 `api_key`、`raw_api_key`、`device_id`、`device_name`、`match_id`、`parse_status` 筛选。
6. 查看订单原始 payload。
7. 删除单条订单。
8. 删除全部订单。
9. 标记是否已经通知客户。
10. 一键复制订单姓名和支付通知消息。

后台订单列表应展示：

- 订单 ID
- 用户 key / 原始 key
- 是否关联用户
- 设备 ID
- 设备名
- 比赛 ID
- 票数
- 姓名
- 区域
- 价格
- 手机号（点击按钮后按订单姓名对应展示）
- 任务类型
- 解析状态
- 解析错误
- 创建时间

## 9.5 比赛公告配置

路径：`/admin-ui/configs`

功能：

1. 创建比赛公告。
2. 更新比赛公告。
3. 删除比赛公告。
4. 查看公告列表。

---

## 10. 公开结果查询

路径：`/query`

功能：

1. 普通用户输入姓名查询当前比赛结果。
2. 查询范围为当前 `match.json` 中的比赛 ID。
3. `order_names` 按 `&` 分隔多个姓名。
4. 输入姓名与拆分后的姓名完全匹配。
5. 返回订单姓名和区域。
6. 同一 IP 每 30 秒只能查询一次。
7. 普通查询用户的访问限流必须使用 Redis 实现，不再使用进程内存字典。

查询结果建议只使用 `parse_status = ok` 或至少能解析出 `order_names` 和 `order_region` 的订单。

## 10.1 Redis 限流需求

公开查询页 `/query` 必须使用 Redis 记录查询频率。

限流规则：

1. 以客户端 IP 作为限流 key。
2. 同一 IP 每 30 秒只能提交一次查询。
3. Redis key 建议格式为 `query:rate_limit:{ip}`。
4. 用户提交查询时，如果 Redis 中存在该 key，则拒绝查询并返回剩余等待时间。
5. 用户提交查询成功后，写入 Redis key，并设置过期时间为 30 秒。
6. 多进程、多容器或多实例部署时，所有实例共用同一个 Redis，保证限流一致。
7. 如果服务端部署在代理或负载均衡后，应优先读取可信的 `X-Forwarded-For` 获取真实 IP。

Redis 异常处理规则：

1. 如果 Redis 短暂不可用，页面不应直接 500。
2. 可选择降级为允许查询，并记录错误日志。
3. 不建议降级为本地内存限流，避免多实例行为不一致。
4. Redis 恢复后继续使用 Redis 限流。

---

## 11. 管理员 API

管理员 API 可继续存在，但本次重构重点是客户端加密接口和后台页面。管理员 API 可根据需要保留或简化。

建议管理员 API：

| 方法 | 路径 | 功能 |
|---|---|---|
| POST | `/admin/user/create` | 创建 / 更新用户 |
| DELETE | `/admin/user/delete` | 删除用户 |
| PUT | `/admin/user/update` | 更新用户 |
| GET | `/admin/user/list` | 查询用户 |
| GET | `/admin/user/devices` | 查询设备 |
| POST | `/admin/match/config` | 创建 / 更新比赛公告 |
| GET | `/admin/orders/list` | 查询订单 |
| GET | `/admin/task/tickets_count` | 查询出票总数 |
| GET | `/admin/orders/ip_count` | 按比赛统计 IP 对应的订单数量 |

管理员 API 可暂时使用 `ADMIN_PASSWORD`，后续建议升级为 Admin Token。

---

## 12. 环境变量

| 环境变量 | 必填 | 说明 |
|---|---|---|
| `DATABASE_URL` | 是 | 数据库连接地址 |
| `AES_KEY` | 是 | AES 密钥 |
| `AES_IV` | 是 | AES-CBC IV |
| `ADMIN_PASSWORD` | 是 | 管理员密码 |
| `ADMIN_UI_SESSION_SECRET` | 是 | 后台 Session 密钥 |
| `MATCH_API_BASE_URL` | 是 | 外部比赛详情接口地址 |
| `MATCH_API_LID2` | 是 | 外部接口参数 |
| `MATCH_API_BEARER` | 是 | 外部接口 Token |
| `REDIS_URL` | 是 | Redis 连接地址，用于普通查询用户限流 |
| `QUERY_COOLDOWN_SECONDS` | 否 | 公开查询限流秒数，默认 30 |
| `SMTP_HOST` | 否 | SMTP 地址 |
| `SMTP_PORT` | 否 | SMTP 端口 |
| `SMTP_USER` | 否 | SMTP 用户 |
| `SMTP_PASSWORD` | 否 | SMTP 密码 |
| `LOG_LEVEL` | 否 | 日志等级 |

旧版 `SIGN_SECRET` 可不再作为新版客户端接口必填项。

---

## 13. 推荐代码结构

```text
app/
├── main.py
├── core/
│   ├── config.py
│   ├── database.py
│   ├── crypto.py
│   ├── responses.py
│   ├── exceptions.py
│   └── logging.py
├── models/
│   ├── user.py
│   ├── device.py
│   ├── order.py
│   └── config.py
├── schemas/
│   ├── encrypted.py
│   ├── auth.py
│   ├── match.py
│   ├── task.py
│   └── admin.py
├── services/
│   ├── auth_service.py
│   ├── match_service.py
│   ├── task_service.py
│   ├── order_service.py
│   ├── rate_limit_service.py
│   └── order_query_service.py
├── repositories/
│   ├── user_repo.py
│   ├── device_repo.py
│   ├── order_repo.py
│   └── config_repo.py
├── routers/
│   ├── auth.py
│   ├── match.py
│   ├── task.py
│   ├── admin.py
│   ├── admin_ui.py
│   └── order_query_ui.py
├── templates/
└── resources/
```

---

## 14. 验收标准

### 14.1 加密协议

- 客户端接口请求体只传 `payload`。
- 服务端能解密 `payload` 并执行业务。
- 服务端响应只返回加密后的 `payload`。
- 解密响应后能得到完整 `{code, msg, data}`。
- 不再只加密 `data` 字段。

### 14.2 订单上报

- 正常订单应成功入库。
- `api_key` 不存在的订单应成功入库。
- 字段缺失的订单应成功入库，并记录 `parse_status` 和 `parse_error`。
- 字段类型异常但可保存的订单应成功入库。
- 订单应保存 `raw_payload`。
- 数据库写入成功后才返回成功。

### 14.3 设备名

- 授权接口传入 `device_name` 时应保存设备名。
- 任务校验传入 `device_name` 时应更新设备名。
- 订单上报传入 `device_name` 时订单应保存设备名快照。
- 后台设备列表应展示设备名。
- 后台订单列表应展示设备名。

### 14.4 后台订单

- 后台能查看全部原始订单。
- 后台能筛选正常订单、异常订单和 unknown key 订单。
- 后台能查看订单原始 payload。
- 后台不会因为去重展示导致误判订单漏掉。
- 后台可按比赛 ID 查看各下单 IP 对应的设备名和订单记录数。
- 后台可按 IP 前三段汇总订单记录数。
- 后台可持久化订单的客户通知状态，并快捷复制姓名和支付通知消息。

### 14.5 公开查询

- 输入存在于当前比赛订单中的姓名，应返回区域。
- 输入不存在姓名，应显示未查询到结果。
- 30 秒内重复查询，应提示限流。
- 公开查询限流必须使用 Redis。
- Redis 中应按客户端 IP 写入限流 key，并设置 30 秒过期时间。
- 多实例部署时，不同 API 实例应共享同一个 Redis 限流结果。
- Redis 短暂不可用时，查询页面不应直接 500，应记录错误日志并按降级策略处理。

---

## 15. 后续优化建议

1. 引入 Alembic 管理数据库迁移。
2. 后续将 AES-CBC 升级为 AES-GCM。
3. 管理员 API 从密码参数升级为 Admin Token。
4. 后台增加订单导出。
5. 后台增加操作日志。
6. 订单原始 payload 可按需做脱敏展示。
